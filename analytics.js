/**
 * Vercel Web Analytics — loaded on all public HTML pages.
 * Requires Web Analytics enabled in the Vercel project dashboard.
 */
import { inject } from './vendor/vercel-analytics.mjs';

inject();
